# 시스템 장애 유형 이론 정리

## 1. 문서 개요

운영체제 위에서 실행되는 프로그램은 CPU, 메모리와 같은 시스템 자원을 사용한다. 프로그램이 자원을 잘못 사용하거나 특정 자원을 지나치게 많이 점유하면 프로그램 자체의 오류를 넘어 **시스템 장애**로 이어질 수 있다.

이 과제에서는 다음 세 가지 장애 유형을 분석한다.

1. **OOM Crash** — 메모리 사용량이 계속 증가하여 메모리 한계를 초과하고 프로세스가 종료되는 장애
2. **CPU Latency** — 특정 프로세스가 CPU를 과도하게 사용하여 처리 지연이나 시스템 부하를 유발하는 장애
3. **Deadlock** — 여러 실행 흐름이 서로가 가진 자원을 기다리면서 영원히 진행하지 못하는 장애

세 장애는 모두 "프로그램이 정상적으로 동작하지 않는다"는 공통점이 있지만, 장애가 발생하는 원인과 관찰되는 시스템 상태는 서로 다르다.

---

# 2. OOM Crash — 메모리 고갈 장애

## 2.1 OOM이란?

OOM은 **Out Of Memory**의 약자로, 프로그램이 사용할 수 있는 메모리가 부족해지는 상황을 의미한다.

프로그램이 실행되면 운영체제는 프로세스에 메모리 공간을 제공한다. 프로그램은 이 공간에 변수, 객체, 데이터 구조 등을 저장한다.

문제는 프로그램이 더 이상 필요하지 않은 메모리를 적절하게 해제하지 않거나, 계속해서 새로운 데이터를 메모리에 저장하는 경우다.

이러한 현상이 지속되면 다음과 같은 흐름이 발생할 수 있다.

```text
프로그램 실행
    ↓
메모리 사용
    ↓
불필요한 데이터가 계속 남음
    ↓
메모리 사용량 증가
    ↓
사용 가능한 메모리 감소
    ↓
메모리 한계 도달
    ↓
프로세스 종료 또는 OOM 처리
```

과제에서는 `agent-leak-app`의 물리 메모리 사용량이 시간에 따라 증가하는 패턴을 `monitor.sh`로 관찰하고, 프로그램 로그를 통해 메모리 임계치 초과 및 `MemoryGuard`에 의한 종료를 확인하도록 요구한다.

---

## 2.2 메모리 누수(Memory Leak)

OOM을 이해하기 위해 먼저 **Memory Leak(메모리 누수)**을 이해해야 한다.

메모리 누수란 프로그램이 더 이상 필요하지 않은 메모리를 계속 점유하여 사용할 수 있는 메모리가 점점 줄어드는 현상을 의미한다.

개념적으로 다음과 같다.

```text
시간 →
────────────────────────────────────>

메모리
사용량       /
           /
         /
       /
     /
   /
 /
────────────────────────────────────
```

예를 들어 프로그램이 반복적으로 데이터를 생성한다고 하자.

```text
1회: 객체 100개 생성
2회: 객체 100개 추가 생성
3회: 객체 100개 추가 생성
...
```

그런데 이전 객체가 더 이상 필요하지 않은데도 계속 참조되고 있다면 메모리에서 제거되지 않는다.

결과적으로:

```text
사용 중인 데이터
      +
더 이상 필요하지 않지만 남아 있는 데이터
      ↓
전체 메모리 사용량 증가
```

다만 **메모리 사용량이 증가한다는 사실 자체가 항상 메모리 누수를 의미하는 것은 아니다.**

캐시, 버퍼, 작업 큐처럼 프로그램이 의도적으로 메모리를 사용하는 경우에도 메모리 사용량은 증가할 수 있다.

따라서 장애 분석에서는 단순히 "메모리가 증가했다"가 아니라,

> 시간이 지나도 메모리 사용량이 계속 증가하고, 필요하지 않은 메모리가 회수되지 않으며, 결국 설정된 한계를 초과하는가?

를 확인해야 한다.

---

## 2.3 운영체제와 프로세스 메모리

프로그램은 운영체제에서 **프로세스(Process)** 형태로 실행된다.

운영체제는 각 프로세스가 사용할 수 있는 메모리 공간을 관리한다.

프로세스 관점에서는 대략 다음과 같은 구조로 생각할 수 있다.

```text
프로세스
├── Code
├── Data
├── Heap
└── Stack
```

특히 프로그램이 동적으로 생성하는 객체나 데이터는 일반적으로 **Heap** 영역과 관련된다.

예를 들어 다음과 같이 계속 객체를 생성한다고 생각해보자.

```python
data = []

while True:
    data.append(create_object())
```

`data`가 계속 객체를 참조하고 있기 때문에 기존 객체를 회수하기 어려워지고 메모리 사용량이 증가할 수 있다.

---

## 2.4 OOM 장애의 원인

OOM 장애의 원인은 크게 다음과 같이 생각할 수 있다.

### ① 메모리 누수

더 이상 사용하지 않는 메모리를 계속 점유한다.

```text
불필요한 객체
      ↓
계속 참조됨
      ↓
메모리 회수 불가
      ↓
메모리 사용량 증가
```

### ② 지나치게 큰 데이터 처리

한 번에 너무 많은 데이터를 메모리에 올리는 경우다.

예를 들어 매우 큰 파일 전체를 한꺼번에 메모리에 적재하는 경우가 있다.

### ③ 캐시 또는 버퍼의 무제한 증가

캐시의 크기를 제한하지 않으면 시간이 지날수록 저장 데이터가 증가할 수 있다.

### ④ 프로세스에 설정된 메모리 한계 초과

프로그램 자체의 메모리 사용량이 시스템 또는 애플리케이션에서 설정한 제한을 넘어설 수도 있다.

과제의 `MEMORY_LIMIT`이 이러한 상황을 실험하기 위한 환경변수다. 과제에서는 `MEMORY_LIMIT`을 변경하여 프로세스가 더 오래 생존하는지 확인하도록 요구한다.

---

## 2.5 OOM 장애를 어떻게 확인하는가?

OOM은 **시간에 따른 메모리 변화**가 중요한 증거다.

과제에서는 다음 증거를 요구한다.

```text
monitor.sh
    ↓
메모리 사용량 지속 증가 확인
    ↓
프로그램 종료 직전 로그 확인
    ↓
Memory limit exceeded...
SELF-TERMINATED...
등의 로그 확인
    ↓
MEMORY_LIMIT 변경
    ↓
재실행
    ↓
Before & After 비교
```

즉, 단순히 프로그램이 종료됐다는 사실만으로 OOM이라고 판단해서는 안 된다.

### 핵심 판단 구조

```text
메모리 사용량 증가
        +
메모리 한계 초과 로그
        +
프로세스 종료
        ↓
OOM 가능성 판단
```

---

## 2.6 OOM의 조치

과제에서 요구하는 직접적인 조치는 `MEMORY_LIMIT`을 조정하는 것이다.

예를 들어:

```text
Before

MEMORY_LIMIT=100
        ↓
메모리 증가
        ↓
100MB 부근에서 종료
```

이후:

```text
After

MEMORY_LIMIT=200
        ↓
더 많은 메모리 사용 가능
        ↓
프로세스 생존 시간 증가
```

중요한 점은 **메모리 제한을 높이는 것이 메모리 누수의 근본적인 해결책은 아니라는 것**이다.

이는 일종의 **Workaround(임시 조치)**에 가깝다.

근본적으로는:

```text
메모리 누수 발생 지점 발견
        ↓
불필요한 객체/데이터의 참조 제거
        ↓
메모리 회수 가능하도록 수정
        ↓
장시간 실행 테스트
```

와 같은 조치가 필요하다.

---

# 3. CPU Latency — CPU 과점유와 처리 지연

## 3.1 CPU 과점유란?

CPU는 프로세스의 명령을 실제로 실행하는 연산 자원이다.

여러 프로세스가 동시에 실행되는 것처럼 보이지만 실제 CPU 코어에서는 운영체제가 실행 시간을 조정하여 각 프로세스에게 CPU를 할당한다.

특정 프로세스가 CPU를 지나치게 많이 사용하면 다른 작업이 CPU를 사용하기 어려워질 수 있다.

예를 들어:

```text
정상 상태

Process A ── CPU 20%
Process B ── CPU 10%
Process C ── CPU 15%
```

에서 특정 프로세스가 갑자기:

```text
Process A ── CPU 95%
```

를 계속 사용한다면 CPU 자원 경쟁이 심해질 수 있다.

---

## 3.2 CPU 사용률의 의미

CPU 사용률은 CPU가 일정 시간 동안 얼마나 바쁘게 작업했는지를 나타내는 지표다.

예를 들어:

```text
CPU Usage
20% → 낮은 사용량
50% → 상당한 사용량
95% → 매우 높은 사용량
```

처럼 볼 수 있다.

하지만 **CPU 사용률이 높다는 사실만으로 장애라고 단정할 수는 없다.**

정상적인 CPU 집약적 작업도 높은 CPU 사용률을 발생시킬 수 있다.

중요한 것은:

```text
높은 CPU 사용률
+
비정상적인 지속
+
응답 지연 또는 시스템 부하
+
프로그램 로그의 관련 증거
```

를 함께 확인하는 것이다.

---

## 3.3 CPU 과점유가 지연을 발생시키는 원리

CPU 자원은 한정되어 있다.

예를 들어 하나의 CPU 코어를 두 프로세스가 사용한다고 하자.

```text
CPU
│
├── Process A
└── Process B
```

A가 CPU를 지나치게 많이 사용하면 B가 CPU를 사용할 수 있는 시간이 줄어든다.

운영체제는 이를 해결하기 위해 프로세스들을 번갈아 실행한다.

```text
시간 →
A A A B A A B A A A B ...
```

이 과정에서 프로세스 전환이 발생하며, CPU가 다른 작업을 수행하기 위해 기다리는 시간이 증가할 수 있다.

결과적으로 CPU를 과도하게 사용하는 프로세스 때문에:

```text
CPU 과점유
    ↓
다른 작업의 CPU 실행 기회 감소
    ↓
처리 지연
    ↓
응답 시간 증가
    ↓
CPU Latency
```

가 발생할 수 있다.

---

## 3.4 CPU 과점유의 대표적인 원인

### ① 무한 루프

가장 직관적인 예다.

```python
while True:
    calculate()
```

종료 조건이나 적절한 대기 없이 계속 연산한다면 CPU를 지속적으로 사용할 수 있다.

### ② 지나치게 많은 계산

복잡한 알고리즘이나 대규모 데이터 처리 때문에 CPU 사용량이 급증할 수 있다.

### ③ 잘못된 Polling

상태가 변경되었는지 계속 확인하는 코드를 너무 빠르게 반복하면 CPU를 불필요하게 사용할 수 있다.

```python
while True:
    if check_status():
        ...
```

적절한 sleep이나 이벤트 기반 구조가 없다면 CPU를 계속 소비할 수 있다.

### ④ 스레드 경쟁

여러 스레드가 동시에 실행되면서 CPU 사용량이 증가할 수도 있다.

---

## 3.5 CPU 장애의 관측

과제에서는 특정 프로세스인 `agent-leak-app`의 CPU 사용률이 급격히 상승하는 구간을 찾아야 한다.

중요한 점은 **시스템 전체 CPU 사용률과 특정 프로세스의 CPU 사용률을 구분하는 것**이다.

예를 들어:

```text
System CPU
────────────────
CPU 60%

Process A
────────────────
CPU 95%
```

라면 특정 프로세스가 CPU를 많이 사용하고 있다는 증거가 된다.

과제에서는 다음 도구를 활용할 수 있다.

```text
monitor.sh
top
ps
```

그리고 종료 로그에서:

```text
WATCHDOG...
SIGTERM
```

등을 확인하여 단순한 프로그램 오류가 아니라 과점유 방지 정책인 **Watchdog**에 의해 종료되었음을 확인한다.

---

## 3.6 Watchdog란?

Watchdog은 시스템이나 프로그램이 특정 조건을 초과하는지 감시하고, 문제가 발생하면 보호 조치를 수행하는 메커니즘이다.

과제에서는 CPU 사용률이 특정 기준을 초과했을 때 Watchdog이 프로세스를 종료하는 구조를 관찰한다.

개념적으로:

```text
프로세스 실행
      ↓
CPU 사용률 감시
      ↓
CPU 사용량이 임계치 초과?
      ↓
Yes
      ↓
Watchdog 동작
      ↓
SIGTERM
      ↓
프로세스 종료
```

여기서 `SIGTERM`은 프로세스에게 종료를 요청하는 Unix 계열의 신호다.

---

## 3.7 CPU 장애의 조치

과제에서는 `CPU_MAX_OCCUPY` 환경변수를 변경하여 결과를 비교한다.

예를 들어:

```text
Before

CPU_MAX_OCCUPY=50%
        ↓
CPU 사용률 80%
        ↓
Watchdog 조건 충족
        ↓
프로세스 종료
```

그리고:

```text
After

CPU_MAX_OCCUPY=90%
        ↓
동일한 CPU 사용량
        ↓
임계치 초과 여부 변화
        ↓
프로세스 생존 시간 또는 종료 여부 변화
```

를 비교한다.

이 역시 **환경 설정을 변경하여 장애 발생 조건을 완화하는 조치**이지, CPU 과점유를 발생시키는 프로그램의 근본적인 원인을 제거한 것은 아니다.

근본 해결책은 CPU를 과도하게 사용하는 코드 자체를 분석하여:

```text
무한 루프 제거
불필요한 polling 감소
알고리즘 개선
작업 분할
적절한 sleep/event 사용
```

등의 방법으로 CPU 사용량을 줄이는 것이다.

---

# 4. Deadlock — 교착상태

## 4.1 Deadlock이란?

Deadlock은 여러 프로세스 또는 스레드가 서로가 가진 자원을 기다리면서 **영원히 진행하지 못하는 상태**다.

쉽게 말하면:

> "내가 가진 것을 네가 먼저 내놓아야 하고, 너는 네가 가진 것을 내가 먼저 내놓아야 한다."

와 같은 상황이다.

예를 들어 두 스레드가 있다고 하자.

```text
Thread A
Lock 1 획득
    ↓
Lock 2 필요
    ↓
Thread B가 가지고 있음
    ↓
대기
```

동시에:

```text
Thread B
Lock 2 획득
    ↓
Lock 1 필요
    ↓
Thread A가 가지고 있음
    ↓
대기
```

그러면:

```text
Thread A → Thread B가 가진 Lock 2를 기다림
Thread B → Thread A가 가진 Lock 1을 기다림
```

두 스레드 모두 영원히 기다리게 된다.

---

# 5. Deadlock의 4가지 필요 조건

Deadlock은 아무 때나 발생하는 것이 아니다.

일반적으로 교착상태가 발생하기 위해서는 다음 네 가지 조건이 동시에 성립해야 한다.

## 5.1 Mutual Exclusion — 상호 배제

하나의 자원을 한 번에 하나의 실행 흐름만 사용할 수 있어야 한다.

예를 들어 하나의 Lock을 동시에 두 스레드가 획득할 수 없다.

```text
Lock 1
  ↓
Thread A 사용 중

Thread B
  ↓
대기
```

---

## 5.2 Hold and Wait — 점유 대기

자원을 이미 가지고 있는 상태에서 다른 자원을 추가로 기다려야 한다.

```text
Thread A
├── Lock 1 보유
└── Lock 2 대기
```

즉, 자신이 가진 자원을 놓지 않은 상태에서 다른 자원을 기다린다.

---

## 5.3 No Preemption — 비선점

다른 스레드가 가지고 있는 자원을 강제로 빼앗을 수 없어야 한다.

즉:

```text
Thread A → Lock 1 보유

Thread B
→ "Lock 1 내놔!"

Thread A
→ 직접 반환할 때까지 강제로 빼앗을 수 없음
```

과 같은 상황이다.

---

## 5.4 Circular Wait — 순환 대기

가장 중요한 개념 중 하나다.

여러 실행 흐름이 원형으로 서로의 자원을 기다리는 상태다.

```text
Thread A
   │
   │ Lock B 기다림
   ↓
Thread B
   │
   │ Lock A 기다림
   ↓
Thread A
```

더 많은 스레드가 있다면:

```text
A → B → C → A
```

처럼 순환 구조가 만들어질 수 있다.

---

# 6. Dining Philosophers Problem

Deadlock을 이해하기 위한 대표적인 모델이 **식사하는 철학자들 문제(Dining Philosophers Problem)**다.

철학자들이 원형 테이블에 앉아 있고, 철학자 사이에 젓가락이 하나씩 있다고 생각한다.

식사를 하려면 젓가락 두 개가 필요하다.

```text
      Philosopher A
       /         \
   Fork 1       Fork 2
     /             \
Philosopher D   Philosopher B
     \             /
   Fork 4       Fork 3
       \         /
      Philosopher C
```

각 철학자가 왼쪽 젓가락을 먼저 가져가면:

```text
A → Fork 1 보유 → Fork 2 대기
B → Fork 2 보유 → Fork 3 대기
C → Fork 3 보유 → Fork 4 대기
D → Fork 4 보유 → Fork 1 대기
```

결과적으로:

```text
A → B → C → D → A
```

라는 순환 대기가 발생한다.

이것이 Deadlock의 대표적인 예다.

---

# 7. Deadlock은 왜 CPU 사용량이 낮을 수 있는가?

Deadlock을 CPU 장애와 구분할 때 매우 중요한 부분이다.

무한 루프에 빠진 프로그램은 일반적으로 계속 CPU를 사용할 수 있다.

반면 Deadlock에 빠진 스레드는 **자원을 기다리는 상태**이기 때문에 CPU를 계속 사용할 필요가 없다.

따라서:

```text
CPU 장애

CPU ↑↑↑
프로그램 계속 실행
```

과 달리:

```text
Deadlock

PID 존재
CPU 변화 거의 없음
MEM 변화 거의 없음
로그 진행 중단
```

같은 상태가 나타날 수 있다.

과제에서도 바로 이 점을 이용하여 Deadlock을 진단하도록 요구한다. 프로세스의 PID는 존재하지만 CPU와 메모리 변화가 없고, 로그가 멈춘 상태를 확인하도록 되어 있다.

---

# 8. Deadlock의 진단 방법

Deadlock에서는 "프로그램이 죽었는가?"가 아니라 **"프로그램이 살아 있지만 진행하지 못하고 있는가?"**를 확인해야 한다.

## 8.1 PID 확인

```bash
ps -ef | grep agent-leak-app
```

프로세스가 존재하는지 확인한다.

```text
PID 존재
    ↓
프로세스 자체는 종료되지 않음
```

---

## 8.2 CPU / 메모리 변화 확인

```bash
top
top -H
ps -L
```

등을 활용하여 프로세스 또는 스레드 상태를 관찰한다.

Deadlock에서는 일반적으로:

```text
CPU 사용량 변화 거의 없음
MEM 변화 거의 없음
```

과 같은 특징을 볼 수 있다.

단, 이것만으로 Deadlock이라고 확정해서는 안 된다.

CPU를 사용하지 않는 다른 대기 상태도 존재하기 때문이다.

---

## 8.3 마지막 로그 확인

프로그램 로그가 다음과 같이 끝났다고 생각해보자.

```text
Thread-1: WAITING FOR RESOURCE-B
Thread-2: WAITING FOR RESOURCE-A
```

이를 앞에서 확인한 Lock 보유 상태와 결합하면:

```text
Thread-1
Resource-A 보유
Resource-B 대기

Thread-2
Resource-B 보유
Resource-A 대기
```

라는 관계를 추론할 수 있다.

이렇게 **로그 + 프로세스 상태 + 스레드 상태**를 결합해야 Deadlock을 논리적으로 증명할 수 있다.

과제 역시 마지막 로그의 `WAITING`, `BLOCKED` 등의 상태를 분석하고 서로 다른 스레드가 상대방의 자원을 기다리는 상황을 근거로 제시하도록 요구한다.

---

# 9. Deadlock의 조치

과제에서는 `MULTI_THREAD_ENABLE` 환경변수를 조정하여 Deadlock의 재현 또는 회피 결과를 비교한다.

개념적으로:

```text
MULTI_THREAD_ENABLE=true
        ↓
여러 스레드 실행
        ↓
Lock 경쟁
        ↓
Deadlock 발생 가능
```

반대로 설정을 변경하면:

```text
MULTI_THREAD_ENABLE=false
        ↓
멀티스레드 실행 조건 제거
        ↓
Lock 경쟁 감소
        ↓
Deadlock 회피
```

와 같은 비교가 가능하다.

하지만 멀티스레드를 끄는 것 역시 근본적인 Deadlock 해결책이라고 보기는 어렵다.

근본적인 해결은 **자원 획득 순서를 통일하거나, Lock 구조를 변경하여 Deadlock의 필요 조건 중 하나 이상을 제거하는 것**이다.

---

# 10. Deadlock을 예방하는 대표적인 방법

## 10.1 Lock 획득 순서 통일

가장 대표적인 방법이다.

모든 스레드가:

```text
Lock A → Lock B
```

순서로만 Lock을 획득하도록 만든다.

그러면:

```text
Thread A: A → B
Thread B: A → B
```

가 되므로 서로 반대 방향으로 Lock을 기다리는 순환 구조가 만들어지기 어려워진다.

---

## 10.2 Lock을 오래 보유하지 않기

Lock을 획득한 뒤 불필요하게 오랫동안 보유하면 다른 스레드가 대기해야 하는 시간이 증가한다.

따라서:

```text
Lock 획득
   ↓
최소한의 작업 수행
   ↓
Lock 반환
```

형태로 Lock의 범위를 줄이는 것이 중요하다.

---

## 10.3 Timeout 사용

무한정 Lock을 기다리지 않고 일정 시간이 지나면 실패하도록 만들 수 있다.

```text
Lock 요청
   ↓
일정 시간 대기
   ↓
획득 실패
   ↓
작업 취소 / 재시도
```

이렇게 하면 무한 대기를 방지할 수 있다.

---

# 11. 세 장애의 비교

| 구분       | OOM Crash       | CPU Latency         | Deadlock              |
| -------- | --------------- | ------------------- | --------------------- |
| 핵심 자원    | 메모리             | CPU                 | Lock / 공유 자원          |
| 핵심 문제    | 메모리 사용량 증가      | CPU 과점유             | 자원 상호 대기              |
| 대표 증상    | 메모리 지속 증가 후 종료  | CPU 급상승, 처리 지연      | 프로세스 생존 + 진행 정지       |
| CPU      | 정상 또는 증가 가능     | 매우 높음               | 낮거나 정체될 수 있음          |
| 메모리      | 지속 증가 가능        | 반드시 증가하지 않음         | 대체로 큰 변화 없음           |
| 로그       | Memory limit 초과 | WATCHDOG / SIGTERM  | WAITING / BLOCKED     |
| 주요 관측 도구 | monitor.sh      | monitor.sh, top, ps | ps, top -H, ps -L     |
| 과제 환경변수  | `MEMORY_LIMIT`  | `CPU_MAX_OCCUPY`    | `MULTI_THREAD_ENABLE` |
| 임시 조치    | 메모리 한도 증가       | CPU 허용 한도 조정        | 멀티스레드 비활성화            |
| 근본적 접근   | 메모리 누수 제거       | CPU 과점유 코드 개선       | Deadlock 구조 제거        |

---

# 12. 장애 분석의 핵심 사고방식

세 장애를 공부할 때 중요한 것은 각각의 증상을 외우는 것이 아니다.

실제 장애 분석에서는 다음과 같은 사고 흐름을 사용한다.

```text
현상 관찰
   ↓
객관적인 데이터 수집
   ↓
시간에 따른 변화 확인
   ↓
프로세스 상태 확인
   ↓
로그와 시스템 상태 연결
   ↓
가능한 원인 추론
   ↓
환경 설정 변경
   ↓
다시 실행
   ↓
Before / After 비교
   ↓
원인과 조치의 관계 검증
```

예를 들어 "프로그램이 종료됐다"는 사실만으로는 부족하다.

### OOM이라면

```text
메모리 ↑
   +
Memory limit exceeded
   +
프로세스 종료
```

라는 증거가 필요하다.

### CPU 장애라면

```text
특정 프로세스 CPU ↑
   +
Watchdog 로그
   +
SIGTERM
```

이라는 관계를 확인한다.

### Deadlock이라면

```text
PID 존재
   +
CPU/MEM 변화 정체
   +
로그 진행 중단
   +
WAITING/BLOCKED
   +
스레드 간 상호 자원 대기
```

라는 관계를 확인한다.

즉, **장애 분석에서 중요한 것은 증상 자체가 아니라 증상과 원인을 연결하는 증거의 연쇄**다.

---

# 13. 과제에서 기억해야 할 핵심

### OOM Crash

**원인**

```text
메모리 사용량이 지속적으로 증가
→ 메모리 한계 초과
→ MemoryGuard에 의한 종료
```

**관찰**

```text
monitor.sh
+ 실행 로그
+ MEMORY_LIMIT 변경 전후
```

**조치**

```text
MEMORY_LIMIT 조정
```

---

### CPU Latency

**원인**

```text
특정 프로세스의 CPU 과점유
→ 다른 작업의 CPU 자원 부족
→ 처리 지연
→ Watchdog 조건 충족
```

**관찰**

```text
top / ps / monitor.sh
+ WATCHDOG 로그
+ SIGTERM
```

**조치**

```text
CPU_MAX_OCCUPY 조정
```

---

### Deadlock

**원인**

```text
여러 스레드가 자원을 점유
→ 서로 필요한 자원을 기다림
→ 순환 대기
→ 영원히 진행하지 못함
```

**관찰**

```text
PID 존재
+ CPU/MEM 정체
+ 로그 중단
+ WAITING/BLOCKED
+ 스레드 간 자원 대기 관계
```

**조치**

```text
MULTI_THREAD_ENABLE 조정
```

---

# 14. 최종적으로 이해해야 하는 것

세 장애를 한 문장씩 정리하면 다음과 같다.

> **OOM**은 "메모리를 계속 사용하다가 한계를 넘는 문제"다.

> **CPU Latency**는 "특정 작업이 CPU를 과도하게 사용하여 처리 지연을 일으키는 문제"다.

> **Deadlock**은 "여러 실행 흐름이 서로가 가진 자원을 기다리면서 영원히 진행하지 못하는 문제"다.

그리고 실제 트러블슈팅에서는 다음 세 가지 질문을 반복해서 던지는 것이 중요하다.

```text
1. 무엇이 비정상인가?
2. 어떤 데이터가 그것을 증명하는가?
3. 그 데이터를 통해 어떤 원인을 추론할 수 있는가?
```

이 세 질문에 답할 수 있다면 이번 과제에서 요구하는 **관제 데이터 → 원인 추론 → 조치 → Before & After 검증 → 기술 리포트 작성**의 전체 흐름을 이해한 것이다.
